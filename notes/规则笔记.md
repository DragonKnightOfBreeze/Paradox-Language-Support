# 规则笔记

## Stellaris

### V3.6.1

`effects.log`中不存在，但`effects.cwt`中存在的`effect`：

- set_agreement_term （忽略，日志输出的问题）
- set_timed_design_flag（删除）
- break_fleet_contract（删除）
- remove_fleet_contract_modifiers（删除）

`triggers.log`中不存在，但`triggers.cwt`中存在的`trigger`：

- check_economic_modifier_for_job（删除）
- count_armies（3.6已移除）