
types = {
    ## type_key_filter <> { tribalgfx nomadgfx }
    type[graphical_culture] = {
        path_strict = yes
        path = "game/common/graphicalculturetypes"
    }
}

graphical_culture = {
    ### The skin tone used for this graphical culture.
    skin_tone = enum[skin_tones]
    
    # TODO: Figure out what this does.
    ## cardinality = 0..1
    ### If yes, no idea what happens. Defaults to "no".
    christian_portraits = bool
}