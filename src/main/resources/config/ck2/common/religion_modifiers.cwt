types = {
    type[religion_modifier] = {
        path_strict = yes
        path = "game/common/religion_modifiers"

        localisation = {
            name = "$"
        }
    }
}

# TODO: see if years only or days/months/years
religion_modifier = {
    ### How much the religious authority changes.
    authority = int

    # TODO: see if other durations work
    ### For how long by default does the modifier last once applied.
    years = int
}