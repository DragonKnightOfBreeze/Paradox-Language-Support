package icu.windea.pls.lang.index.constraints

/**
 * 索引约束。
 *
 * 用于优化和调整索引逻辑，例如配置忽略大小写、提供分片索引，等等。
 *
 * @see ParadoxDefinitionIndexConstraint
 * @see ParadoxLocalisationIndexConstraint
 */
interface ParadoxIndexConstraint<T>
