package icu.windea.pls.lang.actions.csv

import com.intellij.openapi.actionSystem.*
import com.intellij.openapi.project.*
import com.intellij.psi.*
import icu.windea.pls.csv.psi.*

class MoveRowUpAction : ManipulateRowActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvRow>) {
        //TODO 2.0.1-dev
    }
}

class MoveRowDownAction : ManipulateRowActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvRow>) {
        //TODO 2.0.1-dev
    }
}

class InsertRowAboveAction : ManipulateRowActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvRow>) {
        //TODO 2.0.1-dev
    }
}

class InsertRowBelowAction : ManipulateRowActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvRow>) {
        //TODO 2.0.1-dev
    }
}

class SelectRowAction : ManipulateRowActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvRow>) {
        //TODO 2.0.1-dev
    }
}

class RemoveRowAction : ManipulateRowActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvRow>) {
        //TODO 2.0.1-dev
    }
}
