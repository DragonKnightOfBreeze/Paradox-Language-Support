package icu.windea.pls.lang.actions.csv

import com.intellij.openapi.actionSystem.*
import com.intellij.openapi.project.*
import com.intellij.psi.*
import icu.windea.pls.csv.psi.*

class MoveColumnLeftAction : ManipulateColumnActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvColumn>) {
        //TODO 2.0.1-dev
    }
}

class MoveColumnRightAction : ManipulateColumnActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvColumn>) {
        //TODO 2.0.1-dev
    }
}

class InsertColumnLeftAction : ManipulateColumnActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvColumn>) {
        //TODO 2.0.1-dev
    }
}

class InsertColumnRightAction : ManipulateColumnActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvColumn>) {
        //TODO 2.0.1-dev
    }
}

class SelectColumnAction : ManipulateColumnActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvColumn>) {
        //TODO 2.0.1-dev
    }
}

class RemoveColumnAction : ManipulateColumnActionBase() {
    override fun doInvoke(e: AnActionEvent, project: Project, file: PsiFile, elements: Sequence<ParadoxCsvColumn>) {
        //TODO 2.0.1-dev
    }
}
