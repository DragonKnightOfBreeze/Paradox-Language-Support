guide = {
    name = data_type_lession_1
    basic_data_types = {
        d_any = hello_world
        d_any = { hello_world }

        d_bool = yes
        d_bool = no

        d_int = 0
        d_int = 1
        d_int = -1
        d_int = 001

        d_int_ranged = 0
        d_int_ranged = 10
        # d_int_ranged = 100 # number out of range -> lenient matched (reported by inspections)

        d_float = 0
        d_float = 1
        d_float = -1
        d_float = 001
        d_float = 0.0
        d_float = 1.0
        d_float = -1.0
        d_float = 001.0
        d_float = .0 # allowed
        # d_float = 0. # not allowed, `0.` is not a `FLOAT_TOKEN` here

        d_float_ranged = 0
        d_float_ranged = 10
        # d_float_ranged = 100 # number out of range -> lenient matched (reported by inspections)
        d_float_ranged = 0.0
        d_float_ranged = 10.0
        # d_float_ranged = 100.0 # number out of range -> lenient matched (reported by inspections)

        d_scalar = yes # allowed
        d_scalar = 1 # allowed
        d_scalar = scalar

        d_color_field = rgb { 255 0 0  }

        d_color_field_rgb = rgb { 255 0 0 }
    }
}

guide = {
    name = data_type_lession_2
    extra_basic_data_types = {
        d_percentage_field = 10%
        d_percentage_field = 10.0%

        d_int_percentage_field = 10%

        d_date_field = 2200.1.1
        d_date_field = 2200.01.01

        d_date_field_formatted = 2200.01.01
    }
}

guide = {
    name = data_type_lession_3
    complex_data_types = {
        d_definition = hello_world
        d_definition = next_step
        d_definition = on_the_way

        d_localisation = title
        d_localisation = name

        d_synced_localisation = NAME_title
        d_synced_localisation = NAME_name

        d_inline_localisation = name
        d_inline_localisation = "This is a name"

        d_enum_value = garden
        d_enum_value = world

        d_value = awaked
        d_value_set = awaked
        d_dynamic_value = awaked_or_not
    }
}


guide = {
    name = data_type_lession_4
    complex_data_types = {
        d_scope_field = this # lenient match
        d_scope = this # lenient match
        d_scope_character = this # lenient match
        d_scope_group_geography = this # lenient match

        # and more, omitted atm...
    }
}

guide = {
    name = data_type_lession_ex_union
    complex_data_types = {
        d_union_value = yes # `bool`
        d_union_value = world # `enum[guide_example_enum]`
        d_union_value = hello_world # `<guide_example>`
        d_union_value = name_windea # `localisation`
    }
}

guide = {
    name = data_type_lession_ex_pattern
    pattern_aware_data_types = {
        d_template = hello_world__world__day1
        d_template = on_the_way__universe__one_day

        d_glob = hello
        d_glob = hello_world
        d_glob = hello_oh_its_you

        d_ant = say/hello
        d_ant = say/hello_world
        # d_ant = say/hello/world # not matched

        d_regex = hello
        d_regex = hello_world
        d_regex = hello_ahh_its_me
    }
}
