types = {
    type[test_type] = {
        subtype[test_subtype] = {
        }
    }
}

enums = {
    enum[test_enum] = {
        value_a
        value_b
    }
}

values = {
    value[test_value] = {
        dyn_value
    }
}

single_alias[test_single_alias] = {
    alias_name[alias_test] = alias_match_left[alias_test]
}

alias[test_modifier:some_modifier] = float

directive[test_directive] = scalar

links = {
    test_link = {
    }
}

localisation_links = {
    test_loc_link = {
        input_scopes = any
        output_scope = any
    }
}

references = {
    enum_ref = enum[ref_enum]
    value_ref = value[ref_value]
    value_set_ref = value_set[ref_value_set]
    dynamic_value_ref = dynamic_value[ref_dynamic]
    alias_name_ref = alias_name[ref_alias]
    alias_match_ref = alias_match_left[ref_alias_left]
    alias_keys_ref = alias_keys_field[ref_alias_keys]
    single_alias_ref = single_alias_right[ref_single_alias]
    type_ref = <ref_type.ref_subtype>
}
