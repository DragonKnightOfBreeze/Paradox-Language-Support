event_namespace = hello_world

event = {
    id = hello_world.1
    title = hello_world.1.title
    desc = hello_world.1.desc

    immediate = {
        say_hello_world = yes
    }
}

event = {
    id = hello_world.2
    title = hello_world.2.title
    desc = hello_world.2.desc

    immediate = {
        hidden_effect = {
            say_hello_world = yes
        }
    }
}
