directive[definition_injection] = {
    modes = {
        INJECT
        REPLACE
        TRY_INJECT
        REPLACE_OR_CREATE
    }
    relax_modes = { TRY_INJECT REPLACE_OR_CREATE }
    self_subtype_modes = { REPLACE REPLACE_OR_CREATE }
}
