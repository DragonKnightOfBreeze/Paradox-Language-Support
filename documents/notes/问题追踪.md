# 问题追踪

> 此文档用于记录来自调试以及相关项目仓库的各类问题，并且重点记录对于部分疑难问题的分析笔记。这些疑难问题通常与索引和缓存有关。

## 问题记录

### 来自调试

- [x] 需要兼容整行或多行参数值中对双引号的特别转义
- [x] 需要兼容本地化文本中对左方括号的转义（`[[`）
- [ ] 在`PARAM = xxx`后面输入回车会产生不正确的自动缩进（应当与语言注入有关）
- [ ] `QuoteLiteralIntention`和`UnquoteLiteralIntention`不直接适用于用引号括起的参数值中的那些字面量（例如，`p = "\"v\""`中的`\"v\"`）
- [ ] 对于用引号括起的参数值中的那些字面量（例如，`p = "\"v\""`中的`\"v\"`），如果用引号括起，由于已被转义一次，会导致中断规则匹配 - IDEA自身BUG或者IDEA设计如此
- [ ] 进行代码补全时，考虑优先补全那些不需要访问索引的，能够比较快速地计算出结果的项
- [ ] 有时脚本文件中的路径引用会无法解析，不知为何？

### 来自 Github 仓库

- [x] 无法及时检测项目本地的规则文件的更改？ - 基本修复？
- [x] 无法正确注入`on_action`中事件引用的事件类型？ - 应当已解决
- [x] 重新打开项目后，IDE无法完成对脚本文件的解析？ - 未复现，不再考虑 
- [ ] `scope[species]`应当始终可以匹配`this` - 如果无法准确匹配，需要直接匹配同类型的第一个规则 - 待验证
- [x] 作用域上下文的内嵌提示显示有问题 - 应当显示在`possible = {`后面，且要求`{`之后不存在空白以外的字符
- [x] `some_trigger = value`中的`value`不应当被特别高亮

### 来自 CWTools 的 Github 仓库

- [x] [The tool cannot recognize in-script flag variables (Vic3) · Issue #76 · cwtools/cwtools-vscode (github.com)](https://github.com/cwtools/cwtools-vscode/issues/76)
- [x] [[Stellaris\] Could support tech@level grammar? · Issue #58 · cwtools/cwtools-vscode (github.com)](https://github.com/cwtools/cwtools-vscode/issues/58)
- [x] [Parsing issues in Vic3 · Issue #53 · cwtools/cwtools (github.com)](https://github.com/cwtools/cwtools/issues/53)

## 问题分析

### P1 索引时报错 `Indexing process should not rely on non-indexed file data.` ✔ {#p1}

> - 状态：已解决

堆栈跟踪：[stack-trace-1](assets/stack-trace-1.txt)

推测：

索引文件时使用到了未索引的数据。即使使用懒加载的数据，仍然可能无法避免。需要持续追踪与优化。

### P2 `ParadoxExpressionManager.getExpressionReferences` 中使用的缓存可能不一致的问题 ✔ {#p2}

> - 状态：不再复现

堆栈跟踪：[stack-trace-2](assets/stack-trace-2.txt)

推测：

不好复现，可能是因为正在索引，不同线程中得到的来自索引的数据不同，因而最终解析得到的引用（`PsiReference`）列表不一致。

### P3 涉及内联脚本与适用语言注入的参数时，IDE可能卡死的问题 ✔ {#p3}

> - 状态：不再复现

堆栈跟踪：[stack-trace-3](assets/stack-trace-3.txt)

```
is@stellaris:0#0#astral_rift/unlock_from_pool_if_relic_not_owned:if/limit/prev/NOT

WAITING
< icu.windea.pls.config.CwtConfigContext.getConfigs
 < icu.windea.pls.lang.index.ParadoxDynamicValueIndexSupport.indexScriptElement
 < icu.windea.pls.lang.index.ParadoxParameterIndexSupport.indexScriptElement
 < icu.windea.pls.lang.index.ParadoxLocalisationParameterParameterIndexSupport.indexScriptElement
```

结论：

应当是调用`LoadingCache.get`或者`ConcurrentMap.computeIfAbsent`导致，尝试改为调用`ConcurrentMap.getOrPut`，避免改变锁行为

### P4 进行全局代码检查时，偶发误判的 *无法解析的值表达式* 报错 ✔ {#p4}

> - 状态：应当已解决

全局代码检查的结果中，可能会报错 *无法解析值表达式 '20'，期望 '{...}'*。
如果重新打开项目，或者更改文件，则报错消失。

```paradox_script
size = 20
```

```cwt
## cardinality = 0..inf
size = int
## cardinality = 0..inf
size = { min = int max = int }
```

2025.11.08
- 可能来自 `ParadoxScriptExpressionBlockMatchOptimizer.optimize` 的实现或调用
- 涉及这种情况的匹配结果有时是不稳定的

2025.11.10
- 在执行后续优化后，`PlsStates.dynamicContextConfigs` 未正确设置导致的
- 需要考虑执行后续优化的时机

### P5 索引时的偶发报错，涉及解析定义的子类型和内联脚本 ✔ {#p5}

> - 状态：应当已解决

堆栈跟踪：[stack-trace-4](assets/stack-trace-4.txt)

### P6 来自的 `ParadoxTextBasedTargetSearcher` 和 fastutil 的索引越界异常 ✔ {#p6}

> - 状态：已解决

堆栈跟踪：[stack-trace-5](assets/stack-trace-5.txt)

结论：

需要使用并发集合，避免在 IntelliJ 并发检索过程中发生非线程安全的 rehash 异常。

### P7 `Indexing process should not rely on non-indexed file data.` 对于子定义 ✔ {#p7}

> - 状态：已解决

堆栈跟踪：[stack-trace-6](assets/stack-trace-6.txt)

```
	at icu.windea.pls.lang.resolve.ParadoxDefinitionService.resolveSubtypeConfigs(ParadoxDefinitionService.kt:74) <- 可能需要访问尚未索引的父定义
	at icu.windea.pls.lang.resolve.ParadoxDefinitionService.resolveDeclaration(ParadoxDefinitionService.kt:80)
	at icu.windea.pls.model.ParadoxDefinitionInfo.doGetDeclaration(ParadoxDefinitionInfo.kt:96)
	at icu.windea.pls.model.ParadoxDefinitionInfo.getDeclaration(ParadoxDefinitionInfo.kt:67)
	at icu.windea.pls.ep.configContext.CwtBaseConfigContextProvider.getCacheKey(CwtConfigContextProviders.kt:79)
```

结论：

`ParadoxDefinitionService.resolveSubtypeConfigs` 中需要检查 `ParadoxMatchOptions.SkipIndex`，如果已设置则需要避免访问其他索引

### P8 定义信息的缓存不一致的问题 {#p8}

> - 状态：待分析

堆栈跟踪：[stack-trace-7](assets/stack-trace-7.txt)

需要研究，但并不紧急。

### P9 对于对应多个 PSI 引用的脚本表达式（如 `religion:catholic`），光标位置的用法高亮可能不正确 ✔ {#p9}

> - 状态：已解决

```
# {link} = {link}:{definition}
religion = religion:catholic
```

分析：
- 光标放到 `religion:` 上时，可能会错误地高亮 `catholic`
- 光标放到 `catholic` 上时，可能会错误地高亮 `religion:`
- 光标放到 `religion:catholic` 的结尾时，可以正确地高亮 `catholic`
- ~~似乎仅在涉及到对定义的引用时，才可能出现这个问题~~ - 即使对于 `event_target:xxx` 也可能出现
- 可能与 `PsiReference.isReferenceTo` 有关？
- 可能与 `referencesSearch` 有关？
- IDE 有时会报错 `expressionReferences` 缓存不一致，可能与这个有关？

可能与这个堆栈有关：[stack-trace-9](assets/stack-trace-9.txt)

即使对以下文件片段中的命令（如 `GetIcon`），也有可能会错误地高亮。

```
l_simp_chinese:
 job_evosymbiont_plural_with_icon: "[calculator_physicist.GetIcon][calculator_biologist.GetIcon][calculator_engineer.GetIcon]$job_evosymbiont_plural$"
```

最终解决方案：
- 添加 `icu.windea.pls.inject.injectors.fix.IdentifierHighlightingComputerCodeInjector`

### P10 表达式 PSI 引用的缓存不一致的问题 ✔ {#p10}

> - 状态：应当已解决

堆栈跟踪：[stack-trace-8](assets/stack-trace-8.txt)

最终解决方案：
- 对于 `ParadoxExpressionElement.getReferences()` 的实现，不再直接调用 `ReferenceProvidersRegistry.getReferencesFromProviders(element)`，而是改为调用 `ParadoxExpressionManager.getReferences(element)`。

### P11 表达式 PSI 引用的缓存不一致的问题 & `Issue142Test.testVarInEffect` 可能无法通过 ✔ {#p11}

> - 状态：可能已解决

堆栈跟踪：[stack-trace-10](assets/stack-trace-10.txt)

表达式 PSI 引用的缓存不一致，有时会导致 `Issue142Test.testVarInEffect` 无法通过。

最终解决方案：
- 对于 `ParadoxExpressionElement.getReferences()` 的实现，不再直接调用 `ReferenceProvidersRegistry.getReferencesFromProviders(element)`，而是改为调用 `ParadoxExpressionManager.getReferences(element)`。

### P12 java.lang.Throwable: Write-unsafe context! Model changes are allowed from write-safe contexts only. ✔ {#p12}

> - 状态：应当已解决

堆栈跟踪：[stack-trace-11](assets/stack-trace-11.txt)

自插件版本2.1.8开始，由于解析 rootInfo 时不再依赖VFS，应当不会再复现了。

### P13 表达式 PSI 引用的缓存不一致的问题 & `Issue284Test.testScopeLinkInEffect` 可能无法通过 ✔ {#p13}

> - 版本：v2.1.8
> - 日期：2026-05-06
> - 状态：可能已解决

堆栈跟踪：[stack-trace-12](assets/stack-trace-12.txt)

无法稳定复现，需要研究。

推测：
- 在 `SoftConcurrentHashMap` 上实际使用的是 `MutableMap<K, V>.getOrPut`，这是非原子性的。
- 正确的方案是改为使用 `ConcurrentMap<K, V>.getOrPut` 或者 `ConcurrentMap<K, V>.computeIfAbsent`
- ~~需要优化 `SoftMutableMap` 和 `SoftConcurrentHashMap` 的实现，补充缺失的委托方法。~~ 改为实现并应用更通用的 `SoftValue`。

### P14 在事件脚本文件中，变更文件内容后，其中特定部分的作用域推断会发生误报 ✔ {#p14}

> - 版本：v2.1.10
> - 日期：2026-06-17
> - 状态：已解决

无法稳定复现，需要研究。

用例：
- VIC3 `game/events/election_events/communist_fascist_election_events.txt`

推断：
- 应当是以下位置的异常，导致使用默认值 true，最终导致随机的作用域字段表达式错误地匹配了随机的模板表达式，最终导致匹配失败。
- 除了模板表达式以外，当候选项中存在定义、本地化等时，应当也可能出现这种情况。
- 与这里的代码对并发的处理有关。
- 可能与 P4 P10 P11 P14 有关，修复这个问题可能会（至少在一定程度上）一并修复这些问题。

```
java.lang.ClassCastException: java.lang.Boolean cannot be cast to kotlin.jvm.functions.Function0
	at icu.windea.pls.lang.match.ParadoxMatchResult$LazyMatch.doGetCatching(ParadoxMatchResult.kt:89)
	at icu.windea.pls.lang.match.ParadoxMatchResult$LazyMatch.get(ParadoxMatchResult.kt:68)
```

最终解决方案：修复并优化 `ParadoxMatchResult.LazyMatch` 中的并发处理逻辑。

### P15 测试过程中的 `java.lang.AssertionError: Indexing timeout` {#p15}

> - 版本：v3.0.0
> - 日期：2026-07-13
> - 状态：已解决
> - 相关问题：https://github.com/JetBrains/intellij-platform-gradle-plugin/issues/2188

描述：执行平台测试时，在第一个测试用例之后，会发生 `java.lang.AssertionError: Indexing timeout`，导致无限等待。这会导致CI测试最终因超时而失败。

```
[AWT-EventQueue-0]

com.intellij.testFramework.PlatformTestUtil.waitWithEventsDispatching(java.util.function.Supplier<java.lang.String>, java.util.function.BooleanSupplier, int, java.lang.Runnable)
 com.intellij.testFramework.PlatformTestUtil.waitWithEventsDispatching(java.util.function.Supplier<java.lang.String>, java.util.function.BooleanSupplier, int)
  com.intellij.testFramework.PlatformTestUtil.waitWithEventsDispatching(java.lang.String, java.util.function.BooleanSupplier, int)
   com.intellij.testFramework.IndexWaiter.waitNow
// 其中的 condition 在执行第一次测试用例之后便无法获取？

com.intellij.testFramework.IndexWaiter.shouldWait
// scannerExecutor.hasQueuedTasks -> true
// scanningTask.value != null -> true

com.intellij.util.indexing.UnindexedFilesScannerExecutorImpl
com.intellij.util.indexing.UnindexedFilesScannerExecutorImpl.ScheduledScanningTask

// com.intellij.util.indexing.UnindexedFilesScannerExecutorImpl L129 // scanningTask.getAndUpdate { null }
// com.intellij.util.indexing.UnindexedFilesScannerExecutorImpl startTaskInSmartMode L350 // val updated = scanningTask.compareAndSet(old, new)  // 然后就不管 scaningTask 了？或者，为什么上一行指向的循环结束了？
// com.intellij.util.indexing.UnindexedFilesScannerExecutorImpl submitTask // isFullIndexUpdate -> cancelAllTasks -> startTaskInSmartMode

com.intellij.testFramework.LightPlatformTestCase.doSetup // 导致 ->
com.intellij.openapi.roots.impl.ProjectRootManagerImpl.makeRootsChange // 导致 ->
com.intellij.openapi.roots.impl.ProjectRootManagerImpl.fireRootsChanged // 导致 ->
com.intellij.util.indexing.ProjectEntityIndexingService.indexChanges // 导致 ->
com.intellij.util.indexing.UnindexedFilesScanner.queue // 导致 ->
com.intellij.util.indexing.UnindexedFilesScanner.submitTask

ScheduledScanningTask.futureHistory

// com.intellij.util.indexing.UnindexedFilesScannerExecutorImpl L129
```

解决方案：将 Gradle 插件 `org.jetbrains.intellij.platform` 的版本回退到 2.17.0 即可解决。

解决方案（补充）：更新到 2.18.0 或以上版本后，需要在 `build.gradle.kts` 中为 `testImplementation("org.jetbrains.kotlin:kotlin-test-junit")` 显式排除对 Kotlin 标准库（以及协程库）的依赖。

### P16 测试过程中的 `java.lang.NoSuchMethodError: 'kotlin.sequences.Sequence kotlin.sequences.SequencesKt.sequenceOf(java.lang.Object)'` {#p16}

`com.intellij.util.indexing.UnindexedFilesScannerExecutorImpl` L160 可能会抛出异常：[stack-trace-202607132250.txt](assets/stack-trace-202607132250.txt)

> - 版本：v3.0.0
> - 日期：2026-07-13
> - 状态：可忽略

分析：
- `kotlin.sequences.Sequence kotlin.sequences.SequencesKt.sequenceOf(java.lang.Object)` 是 kotlin 2.2 才存在的，而我们的IDE最低兼容版本是2025.2，因此使用的 kotlin 版本是 2.1.20……
- 这个问题应当可以忽略。但是因为会输出到测试日志中，又不太能忽略……
- 参见：https://plugins.jetbrains.com/docs/intellij/using-kotlin.html#bundled-stdlib-versions

### P17 启动IDE时报错 `Cannot find scheme: Islands Dark from plugins: icu.windea.pls;icu.windea.pls` ✔ {#p17}

> - 版本：v3.0.2
> - 日期：2026-09-02
> - 状态：可忽略

```
2026-09-02 18:52:26,935 [  23976]   WARN - #c.i.o.e.c.i.EditorColorsManagerImpl - Cannot find scheme: Islands Dark from plugins: icu.windea.pls;icu.windea.pls
```

分析：
- 这可能与 `runIde` 时使用的 IDE 版本有关，需要使用最新版本的 IDE 验证。

结论：
- 经验证，在 IDEA 2026.2.1 上不存在此问题（Color Scheme 设置页面的代码高亮符合预期）。应当是 IDE 版本兼容性问题，可以忽略。

### P18 启动IDE时的明显卡顿 ✔ {#p18}

> - 版本：v3.0.2
> - 日期：2026-09-02
> - 状态：可忽略 & 已优化

线程转储：[thread-dump-1871846717.txt](assets/thread-dump-1871846717.txt)

分析：
- 可能与规则分组的初始化有关。
- 需要 profiling。

```
(total time)
icu.windea.pls.config.config.CwtFileConfigResolver.resolve(VirtualFile, CwtConfigGroup, String) - 317.88s
icu.windea.pls.config.config.CwtFileConfigResolver.resolve(CwtFile, CwtConfigGroup, String) - 315.441s
icu.windea.pls.config.util.CwtConfigResolverManager.getConfigs(PsiElement, CwtFile, CwtConfigGroup) - 298.793s
icu.windea.pls.config.config.CwtPropertyConfigResolver.resolve(CwtProperty, CwtFile, CwtConfigGroup) - 296.614s
icu.windea.pls.config.util.CwtConfigResolverManager.getOptionConfigs(CwtMember, CwtConfigGroup) - 279.015s
icu.windea.pls.cwt.CwtParserDefinition.createElement(ASTNode) - 267.685ss // 需要研究
-type == OPTION - 267.6s // 疑惑？
icu.windea.pls.ep.config.configGroup.CwtFileBasedConfigGroupProcessor.process(CwtConfigGroup, Continuation) - 71.264s

icu.windea.pls.config.config.CwtFileConfigResolver.resolve(VirtualFile, CwtConfigGroup, String) - 317.88s
>icu.windea.pls.config.config.CwtPropertyConfigResolver.resolve(CwtProperty, CwtFile, CwtConfigGroup) - 296.614s (collapsed)
>-icu.windea.pls.config.util.CwtConfigResolverManager.getOptionConfigs(CwtMember, CwtConfigGroup) - 277.964s
>--icu.windea.pls.core.PlatformExtensionsKt.forEachChild() - 273.248s
>---com.intellij.psi.impl.source.tree.LeafPsiElement.getNextSibling() - 269.343s
>--->com.intellij.psi.impl.source.tree.CompositeElement.getPsi() - 269.343s
>--->-com.intellij.psi.impl.source.tree.CompositeElement.createPsiNoLock() - 267.6s
>--->--icu.windea.pls.cwt.CwtParserDefinition.createElement(ASTNode) - 267.6s
>--->---icu.windea.pls.cwt.psi.CwtElementTypes$Factory.createElement(ASTNode) - 267.6s (cpu time: <<51ms, total time: 267.6s)
// 这是为什么？
// 这样来看，IDE 冻结的根源可能完全不是骨规则分组的初始化，而是其他地方
```

初步结论：
- 经验证，在 IDEA 2026.2.1 上不存在此问题（至少数秒内就能一切就绪）。可能是 IDE 版本兼容性问题。
- 相关的分析笔记：[分析_启动冻结_线程转储1871846717_3.0.2.md](../ai-notes/analysis/分析_启动冻结_线程转储1871846717_3.0.2.md)

后续建议（来自 deepseek）：
- 若需要兼容 2025.2，可考虑防御性优化：把逐文件 `readAction` 合并为分组/批量读动作；项目打开时懒初始化非 core 游戏分组；限制 `forEachConcurrent` 的并发度。
- 验证手段：在 2025.2 上用 async-profiler wall 模式复现，确认 `RunSuspend.await`/`InternalReadAction.readLoop` 的等待占比。
- 若不考虑 2025.2 的兼容性，可参照 P17 的处理方式，将其标记为 IDE 版本兼容性问题而忽略。

后续优化：
- [x] 减少 `CwtFileBasedConfigGroupProcessor` 中的 `readAction` 的数量。每次处理时仅按照步骤分为总计4个。不再在解析和处理文件时，分别对每个文件执行1次 `readAction`。

### P19 与 `ParadoxLocalisationFindUsagesProvider` 有关的 SOF {#p19}

> - 版本：v3.0.2
> - 日期：2026-09-02
> - 状态：TODO

堆栈跟踪：[stack-trace-p19.txt](assets/stack-trace-p19.txt)

初步结论：
- 直接将 `FindUsagesProvider` 中的相关方法委托给 `ElementDescriptionUtil.getElementDescription` 可能导致意外的 SOF。考虑显式检查或显式实现。
- 相关的分析笔记：[分析_本地化查找用法SOF_P19_3.0.2.md](../ai-notes/analysis/分析_本地化查找用法SOF_P19_3.0.2.md)。

修复方向建议（来自 deepseek）：
1. **最稳妥**：`getType`/`getNodeText` 不再无条件委托；对 EP 未覆盖的元素返回自带默认值（如 `TypePresentationService` 结果或固定字符串）。
2. 或：在 `ParadoxLocalisationElementDescriptionProvider` 中为 key/locale/parameter/command_text/concept_name/text/value 等元素增加兜底非空描述。
3. 或：在 `getType`/`getNodeText` 入口加线程局部递归保护（最小改动，但治标）。
4. 其他三个语言的 provider 同步处理。

后续优化：
- [x] 重构和完善各个 `ElementDescriptionProvider` 和 `FindUsagesProvider`，避免意外的 SOF，优化描述。

### P20 脚本文件中存在的未预期的语法错误 {#p20}

```paradox_script
$PARAM$
```

```paradox_script
// stellaris: common/scripted_effects/psionic_auras_scripted_effects.txt
on_psionic_aura_spawned = {
	aura_owner = { set_visited = prev }
	clear_psionic_aura_visuals = yes # Visual cleanup
	spawn_psionic_aura_object = {
		TYPE = $TYPE$
		LEVEL = 1
	}
	[[!NO_IMMUNE]
		set_timed_star_flag = {
			flag = newly_spawned_psionic_aura
			months = 6
		}
	] # <- expect `RIGHT_BRACKET`, actual `STRING_TOKEN`
}
```