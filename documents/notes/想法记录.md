# 想法记录

> 此文档用于记录随时产生的、主要以待办事项的形式出现的想法，并且主要按日期归类。此文档并不会包括所有形式的想法，也不会包括所有已记录的待办事项。

## 长期 {#long-term}

> 此章节包含作为长期任务或未来愿景的待办事项。

### 文档与参考

- [ ] P2/MS 新增与补充关于复杂表达式的参考文档
- [ ] P2/MS 新增与补充关于常见术语（IDE/领域）的参考文档
- [ ] P4/MS/CONSIDER 新增参考文档：功能速查表（可从 XML 配置文件提取，可借助 AI）
- [ ] P3 清理、补充与完善 complexExpression 的文档注释

### 进阶与愿景

- [ ] P3/CONSIDER 重构或重新编写代码，以更好地生成与合并规则，便于维护
  - 如果要考虑严谨性、可扩展性和可维护性的话，事情会变得非常复杂。
  - 不能简单地只设计一个 generator 组件，考虑分成 dataExtractor/dataMerger/generator 等组件，并且拥有额外的工具类和上层组件。
  - 后续改进时，可以考虑引入AI。
- [ ] DELAY 让 AI 有能力自行维护规则文件，并且尽可能自动化
- [ ] DELAY 需要使用 WebView 渲染的高级可视化功能

## 候选 {#candidates}

> 此章节包含暂未纳入到明确的版本计划中，或者暂未有一个明确的解决方案的待办事项。

### 语言功能支持

- [ ] P4/CONSIDER 适用于 CWT 语言、脚本语言的结构化搜索
- [ ] P4 通过新引入的 EP，提供更多标准/特殊/定制化的层级视图：
  - EP 接口：`icu.windea.pls.ep.hierarchy.ParadoxDefinitionCallHierarchyViewProvider`
  - 两种主要类别：过滤型（仅显示定义节点，过滤要显示的定义类型）与关系型（通常仅显示定义节点，可以更改子节点的获取方式）

### 语言构造支持

- [ ] P2/MS 支持内联定义（即通过内联脚本声明的定义，其名字可以取决于传入的参数值） [#194](https://github.com/DragonKnightOfBreeze/Paradox-Language-Support/issues/194)
- [ ] P2/MS 支持匹配带表达式前缀的脚本表达式（"表达式前缀"指目标脚本表达式之前紧邻的作为前缀的字符串，例如 `k = list v` 中的 `list`） ([#263](https://github.com/DragonKnightOfBreeze/Paradox-Language-Support/issues/263))

### 语法层、分析层、语义层

- [ ] P3 事件链解析应能跨越 scripted_effect
- [ ] P4 为了支持更准确的作用域推断，可能需要在构建合并索引时收集更多数据
- [ ] P3 对于作为上下文的规则以及匹配的规则，有时需要展开并集（`union[x]`）和别名（`alias_keys_field[x]`）

### 规则系统

- [ ] P3 开放内部规则：允许自定义除了 schema 之外的所有现有内部规则类型，考虑新增规则类型 liveTemplateSettings
  - 这些特殊规则的变化基本上不会导致刷新与重新解析，以后可以考虑优化相关代码。
  - 考虑借助 AI 补充更多默认提供的设置规则。
- [ ] P3 对于作为上下文的规则以及匹配的规则，有时需要展开并集（`union[x]`）和别名（`alias_keys_field[x]`）

### 图片处理

- [ ] P2 图片支持：考虑内置解析代码，移除对外部库的依赖，移除相关的代码注入器
- [ ] P4 图片支持：提供更加规范化的API，用于实现切分图片、按层级堆叠图片等功能

### 工具与集成

- [ ] P4 开发用功能（可以考虑提供内部动作）：从本地已安装的所有游戏目录，检测所有图片文件，包括详细列表、数量统计、以及用到的 DDS 格式的统计
- [ ] P4 开发用功能（可以考虑提供内部动作）：从本地已安装的所有游戏目录，检测所有脚本文件和本地化文件，包括详细列表、数量统计、行数统计（行数、非空行、非空行非注释行）
- [ ] P3 开发用功能（可以考虑提取内部动作）：可配置式地直接调用渲染器，检查渲染后的结果
- [ ] P4 脚本文件、本地化文件、CSV 文件的序列化器与反序列化器：从 PSI 转化为一种更加 JSON 友好的格式，并且支持多种配置项，以兼容不同的格式偏好
- [ ] P4/MAYBE 提供实用工具动作的 EP 接口，以及一组预定义的实用工具动作：
  - EP 接口：`icu.windea.pls.ep.tool.ExtraUtilityProvider`
  - 设置类：`icu.windea.pls.lang.tool.ExtraUtilitySettings`
  - 服务类：`icu.windea.pls.lang.tool.ExtraUtilityService`
  - 预设：打包模组（可配置）/ 审计游戏和模组文件（按查询作用域）/ 调用渲染器（不确定）
- [ ] P4 重构和扩展基于Diagrams插件/调用层级的图表功能
  - 优化相关的设置页面，提供更好的、更易扩展的配置方式。
  - 考虑在已有EP的基础上再次提取插件特定的EP。
  - 借助AI，收集想法，考虑提供更多种类的图表。
- [ ] P4/DELAY 除了科技树与事件树以外的更多图表类型
  - 主要面向 Stellaris 以外的游戏，可以考虑AI协助。

### 其他

- [ ] P4/MAYBE 提供特殊的工具动作，以对当前文件设置或清空注入的文件信息
- [ ] P3 重构/重新设计插件设置的数据类和页面

## 按日期 {#by-date}

> 此章节包含按日期补充和整理的代码事项，按需标记优先级、标签和版本号。

### 2026-03-10

- [ ] P2 图片支持：考虑内置解析代码，移除对外部库的依赖，移除相关的代码注入器
- [ ] P3 开放内部规则：允许自定义除了 schema 之外的所有现有内部规则类型，考虑新增规则类型 liveTemplateSettings
  - 这些特殊规则的变化基本上不会导致刷新与重新解析，以后可以考虑优化相关代码。
  - 考虑借助 AI 补充更多默认提供的设置规则。
- [x] P4 代码注入器中出错时的优雅降级
  - 如果在执行注入的代码时出错，采用更理想的方案：忽略，且仅在第一次报错时打印警告日志。
- [ ] P4 开发用功能（可以考虑提供内部动作）：从本地已安装的所有游戏目录，检测所有图片文件：包括详细列表、数量统计、以及用到的DDS格式的统计。
- [ ] P4 开发用功能（可以考虑提供内部动作）：从本地已安装的所有游戏目录，检测所有脚本文件和本地化文件：包括详细列表、数量统计、行数统计（行数、非空行、非空行非注释行）。

### 2026-03-11

- [x] P2 开发用功能：完善用于统计代码状态和规则状态的py脚本。也检测注释行数。
- [x] P2 完善目录补全 - 需要兼容不同游戏的各种 entry（入口名），根目录和入口目录中的行为可能是不同的。也涉及对项目本地的规则目录和各个规则分组目录的补全。
- [ ] P3 开发用功能（可以考虑提取内部动作）：可配置式地直接调用渲染器，检查渲染后的结果。
- [x] P4 `ChangeDefinitionInjectionModeIntentiom` - 交给AI完成
- [x] P4 `Change...Intention` 的集成测试 - 交给AI完成

### 2026-03-12

- [ ] P4 图片支持：提供更加规范化的API，用于实现切分图片、按层级堆叠图片等功能

### 2026-03-14

- [ ] DELAY 让AI有能力自行维护规则文件，并且尽可能自动化
- [ ] DELAY 需要使用 WebView 渲染的高级可视化功能
- [ ] P4/DELAY 除了科技树与事件树以外的更多图表类型 - 主要面向 Stellaris 以外的游戏，可以考虑AI协助
- [x] P2 开发用功能：一些有趣的用于审计并报告特定文件的状态（项目代码文件/规则文件/游戏文件的行数信息）的py脚本

### 2026-03-15

- [x] P1 提取复杂表达式的综合属性评估器。目前允许传入任意节点。
- [x] P1 主要使用合并索引的声明规则综合属性评估器，用于在文件级别（乃至脚本成员级别）直接快速返回。
- [x] P1 通过提取和检查综合属性，以及其他一些启发式的策略，优化插件在索引时的性能（主要是构建合并索引时的性能）。
- [ ] DELAY 评估方案C（在脚本成员级别检查继承属性）是否有必要应用以及效果。
- [x] P1 应用方案D（在定义级别再次检查综合属性），评估效果。
- [ ] P4 评估是否可以通过 flag-mask 模式进一步压缩缓存数据的内存占用。

### 2026-03-17+

- [x] P2 对定义索引进行更加激进的优化，减少 PSI 遍历次数。
- [x] P4 合并索引同样要兼容定义注入的声明，尽管这影响不大。
- [ ] P4 为了支持更准确的作用域推断，可能需要在构建合并索引时收集更多数据。

### ［2026年3月18日］

- [ ] P3 清理、补充与完善 complexExpression 的文档注释。
- [x] P3 清理、补充与完善 config 的文档注释。
- [x] P3 清理、补充与完善 configExpression 的文档注释。
- [x] P3 补充与完善 dataType 的文档注释。

### ［2026年3月20日］

- [x] P2 修复内联重构的bug，并补充测试（`buildList` 有时候还挺坑的……）

### ［2026年3月24日］

- [ ] P4/CONSIDER 适用于 CWT 语言、脚本语言（考虑：本地化语言）的结构化搜索。
  - 看起来JB官方都没有提供JSON和YAML的结构化搜索，我真的需要提供这个吗？
- [x] P4 在规则分组数据加载完毕后，通过 fastutil 集合的 trim 等方法整理其占用内存空间。

### ［2026年3月26日］

- [x] P2 在 IDEA 新版本（2026.1），无法通过 `BrowserUtil` 从 Steam URL 协议的链接预期地跳转到 Steam 客户端，或者启动游戏。
- [x] P2 IDEA 2026.1 的兼容性修复和优化。
- [x] P3 为 `launcher-settings.json` 和 `metadata.json` 提供内置的 json schema

### ［2026年3月31日］

- [x] P2 在 `build.gradle.kts` 中正确配置 pluginVerification，避免CI中的相关任务静默失败。

### ［2026年4月3日］

- [x] P2 再次重构渲染器组件
  - Renderer - 入口
  - RenderSettings - 数据类，需要传递给Renderer和RenderContext
  - RenderContext - 非纯粹的模型类，访问者模式+模版模式
  - 相关类/接口放到同一代码文件中

### ［2026年4月6日］

- [x] P3 本地化文件中的快速复制文本的意向
  - 复制当前语言环境下的所有本地化（条目）到剪贴板（带语言环境前缀）
  - 复制当前语言环境下的所有本地化（条目）到剪贴板（不带语言环境前缀）
- [x] P3 对于通过“生成本地化动作”创建并打开的临时本地化文件：
  - 直接提供编辑器通知，以及在右侧提供对应的动作，以便快速复制文本
  - 可以配置是否按字母顺序排序本地化（条目），以及小节之间是否加空行
- [ ] P4 脚本文件、本地化文件、CSV文件的序列化器与反序列化器。从PSI转化为一种更加JSON友好的格式。并且支持多种配置项，以兼容不同的格式偏好。

### ［2026年4月13日］

- [x] P2 消除或重构initializer组件 & `PlsDataPathService`

### ［2026年4月17日］

- [x] P2 模组描述符文件（在项目视图以及其他地方中）的图标与显示文本不符合预期的回归BUG

### ［2026年4月20日］

- [x] P3 `CwtDefineConfig` - 为定值提供专门的规则以及基于此的语言功能。
- [x] P3 接上，在解析类型规则和声明规则时，强制（目前可以硬编码）排除定值和内联脚本相关的。
  - 引入 `configFilterProvider`

### ［2026年4月21日］

- [x] P4/DELAY ~~验证和评估内联数学表达式的语法是否需要按运算符顺序展开节点，并同步调整评估器的代码。~~ - 目前是用堆栈维护评估器状态的。
- [x] P2 color scheme - 优化/整理配色选项和预览文本
- [x] P3 检查设置页面，优化文字表述，修正错误
- [x] P3 为规则选项提供文档注释，基于schema规则文件。
- [x] P3 在schema规则文件中补充文档注释。
- [x] P3 清理和优化各个代码检查的检查项描述和报错描述。
- [ ] P3/DELAY 3.2.0 （参考Rust插件的宏展开的相关功能）提供展开scripted trigger/scripted effect/inline script（乃至定义注入）的两种意向动作（递归+单步）
  - Rust插件的参考实现：弹出一个包含编辑器的popup（无顶栏，非dialog）
  - 暂不考虑提供装订线图标（Rust插件会在光标位于当行时提供）
  - 暂不考虑在预览中适用语义高亮（Rust插件当前未适用）
- [x] P3 如果用户在内联数学表达式中引入封装变量引用时使用了 `@var`（而非正确的 `var`），可以考虑在语法层面兼容，但是通过annotator提供报错和快速修复。
- [ ] P2/MS 3.1.0 重构复杂表达式的API，尤其是解析逻辑与校验逻辑。
  - 关键词：Token、TokenStream、Scanner、Resolver
  - 关键词：Visitor、Validator?、Evaluator?
  - 考虑将相关代码移到 `icu.windea.pls.expression`

### ［2026年4月27日］

- [x] P1 适用 `FileRenderCodeInjector` 时，如果需要解析 rootInfo，多次调用 `ParadoxMetadataService.getLauncherSettingsJsonFile` 可能导致 IDE 冻结一段时间

### ［2026年5月1日］

- [x] P2 重构 rootInfo rootMetadata rootMetadataInfo 的相关逻辑
  - 解析这些数据时，不应也不需要依赖VFS，因此也无需 readAction。
  - 直接基于 `Path` 定位文件，解析时不需要依赖文件的PSI。
- [ ] P4 重构和扩展基于Diagrams插件/调用层级的图表功能
  - 优化相关的设置页面，提供更好的、更易扩展的配置方式。
  - 考虑在已有EP的基础上再次提取插件特定的EP。
  - 借助AI，收集想法，考虑提供更多种类的图表。
- [ ] P3 如果tiger输出的json中提供了快速修复信息，考虑完善集成以适用 - 目前看起来并未提供
- [ ] P2/MS 3.1.0 优化对复杂表达式的支持：优化对动态链接节点中传入的字面量的处理逻辑
- [ ] P2/MS 支持匹配带表达式前缀的脚本表达式（"表达式前缀"指目标脚本表达式之前紧邻的作为前缀的字符串，例如 `k = list v` 中的 `list`） (#263)

### ［2026年5月2日］

- [ ] P3 重构/重新设计插件设置的数据类和页面
- [ ] P3 再次验证索引游戏目录时的性能，检查是否存在性能回退

##［2026年5月4日］

- [x] P4 对代码补全系统的重构
  - 改为传递专门的、只读的上下文对象，而非直接传递processingContext
  - 按需复制上下文对象，以切换上下文
- [x] P4 对查询和查询器的重构
  - 改为传递专门的、只读的上下文对象，而非直接传递searchParameters
  - 按需复制上下文对象，以切换上下文
  - 重构和清理代码，以便后续进一步重构，以允许更灵活的查询
- [x] P2 增强对定值（define）的支持
  - [x] 特殊的图标、高亮、快速文档
  - [x] 代码补全（适用于声明处）
  - [x] 导航动作、装订线图标和实现查询（仅适用于定值变量）
  - [x] 相关代码检查（重载、不正确的重载）（仅适用于定值变量）
  - [x] 引入定值规则（defineConfig），提供快速文档文本和规则上下文
  - [x] 适配相关规则的导航（relatedConfigProvider）
- [x] P3 将 directiveConfig重命名为macroConfig（对齐Rust）

### ［2026年5月5日］

- [x] P3 #314 支持在 rowConfig 中指定列的数据表达式时，使用 `value[x]`、`value_set[x]`等，从而将匹配的列解析为单个的动态值

### ［2026年5月6日］

- [x] P2 提供工具动作，弹出对话框，以便快速复制各种路径与链接。
- [ ] P3/CONSIDER 重构或重新编写代码，以更好地生成与合并规则，便于维护
  - 如果要考虑严谨性、可扩展性和可维护性的话，事情会变得非常复杂。
  - 不能简单地只设计一个 generator 组件，考虑分成 dataExtractor/dataMerger/generator 等组件，并且拥有额外的工具类和上层组件。
  - 后续改进时，可以考虑引入AI。

### ［2026年5月11日］

- [x] P3 增强对 mesh locator 的支持
  - 将这些引用视为动态引用，尽管其声明实际上位于 .mesh 文件中。
- [x] P3 增强对 shader effect 的支持
  - 将这些引用视为动态引用，尽管其声明实际上位于 .shader 文件中。

### ［2026年5月19日］

- [ ] P4/MS/CONSIDER 新增参考文档：功能速查表
  - 可从xml配置文件提取，可借助AI

### ［2026年5月20日］

- [x] P3 2.1.9 同步和补充对 `SpecialPathservice` 和 `SpecialUrlService` 的测试（仅打印）。

### ［2026年6月9日］

- [ ] P4/MAYBE 提供实用工具动作的EP接口，以及一组预定义的实用工具动作
  - EP 接口：`icu.windea.pls.ep.tool.ExtraUtilityProvider`
  - 设置类：`icu.windea.pls.lang.tool.ExtraUtilitySettings`
  - 服务类：`icu.windea.pls.lang.tool.ExtraUtilityService`
  - 预设：打包模组（可配置）/审计游戏和模组文件（按查询作用域）/调用渲染器（不确定）
- [x] P2 2.1.10 重构 path/expression/complexExpression/config/configExpression 的解析器与解析的相关代码（改为：模型接口上的静态解析方法 + 委托给解析器类的对应方法）
- [x] P2 2.1.10 重构 expressionSupport 以及相关代码（精简与规范化）
- [x] P3 2.1.10 重构代码补全系统的相关代码（改为：传递专门的、只读的上下文对象+存在全局上下文/动态上下文/特定上下文，需要一定的设计工作）
- [x] P3 2.1.10 提供动作，以进行定义/定义注入之间的差异比较。
- [x] P2/MS 2.2.0 完善对 CSV 文件与 rowConfig 的支持 (#314)
  - [x] 允许在 CSV 文件中声明复杂枚举值（提案：列规则上的 `## declare_complex_enum = x`）。
  - [x] （已移除）~~允许在 CSV 文件中声明定义（提案：列规则上的 `## declare_type = x`）。~~
    - 将一整行视为定义声明，将某个列视为指定了定义名的列。
    - 需要架构上的大幅重构，并且涉及一些领域设计上的问题，因此暂不考虑。
  - [x] 为 rowConfig 添加字段 type，可选值包括 key/index，默认 key。表示是按键还是按索引来匹配行中的每一列。
    - 表头列的列名（columnKey/columnName）可以重复，因此在解析规则数据和语义解析时，可能需要基于列索引，而非列名。
  - [x] 为 rowConfig 添加字段 skip_last_row 和 skip_last_column，值为布尔值。表示匹配与解析时是否忽略最后一列/最后一行。
    - 表头列的最后一列，以及文件中的最后一行，可能需要在语义解析时忽略，因此需要提供对应的规则字段。
  - [x] （已移除）~~考虑将从（脚本/CSV）表达式解析为复杂枚举值的表达式引用（即PSI引用）的逻辑，提取为需要置顶的 expressionSupport 的EP实现。~~
    - 后续也可以考虑以类似方式处理各种特殊标签。
    - 目前不采用，对于特殊标签和复杂枚举值声明，使用需要提前处理、可能不依赖匹配的规则的特殊硬编码逻辑。
  - [x] 调整相关的代码检查的代码。

### ［2026年6月10日］

- [ ] P4 通过新引入的EP，提供更多标准/特殊/定制化的层级视图
  - EP 接口：`icu.windea.pls.ep.hierarchy.ParadoxDefinitionCallHeirarchyViewProvider`
  - 两种主要类别：过滤型  VS 关系型。
  - 前者仅显示定义节点，并且只是过滤要显示的定义类型。
  - 或者通常仅显示定义节点，可以更改子节点的获取方式。
- [x] P4/2.1.10 借助AI，提供更多 specialPathProvider 和 specialUrlProvider 的默认EP实现

### ［2026年6月11日］

- [x] P2 补充维护者笔记，公开路线图
- [x] P2/MS 3.0.0 修订参考文档，尤其是各个参考手册（修正事实性错误，补充必要的细节）
- [x] P2/MS 3.0.0 改进与补充 `README.md`
  - 叙事/问题解决/FAQ/已知限制/……
- [x] P2/MS 3.0.0 新增与补充 `CONTRIBUTING.md`
  - 概述，说明如何贡献代码/文档/规则（步骤/建议/复杂性警告）
- [x] P2/MS 3.0.0 改进与补充 `CONTRIBUTORS.md`
  - 概述，列出主要贡献者（代码/文档/规则/特别感谢/其他）
- [ ] P2/MS 3.2.0 支持内联定义（索引/查询/语义信息/引用解析/快速文档内嵌提示/……）
  - 即通过内联脚本声明的定义，其名字可以取决于传入的参数值
- [ ] P3 3.2.0 实现内联脚本的展开评估器，提供对应的意向动作（单步展开/递归）
  - 点击打开非阻塞的对话框，可以复制文本，可以打开为临时文件
  - 保留处理后仍然带参数的用法
  - 忽略存在递归的用法，并且给出警告（注释）
- [ ] P3 3.2.0 实现scripted_trigger/scripted_effect 的展开评估器，提供对应的意向动作（单步展开/递归）
  - 详细说明同上
- [ ] P3 3.2.0 实现定义注入的展开评估器，提供对应的意向动作（合并当前/合并所有）
  - 点击打开非阻塞的对话框，可以复制文本，可以打开为临时文件
- [ ] P3/MAYBE 3.1.0 实现 script_value 的评估器，提供对应的意向动作（求职数值结果）
  - 这可能需要依赖游戏运行时的上下文（此时显示为动态结果/条件结果）
- [ ] P4/MAYBE 提供特殊的工具动作，以对当前文件设置或清空注入的文件信息

### ［2026年6月12日］

- [x] P2 2.1.10 支持形如 `event_target:target@root` 的动态链接节点
  - 可在作用域字段表达式（scopeFieldExpression）和值字段表达式（valueFieldExpression）中使用，适用于传入的任意类型的动态值
- [x] P2 2.1.10 支持标签集表达式（tagsExpression）
  - 写：`set_flags = "tag1,tag2"`
  - 读：`required_flags = "tag1,not(tag2)"`
  - `tag1` 和 `tag2` 视为标识符（动态值），`not` 视为关键字，允许多余的空白
  - 要求必须用双引号括起，否则给出警告
- [x] P2 2.1.10 ~~支持数组定值引用表达式（arrayDefineReferenceExpression）~~
  - 格式：`array_define:Namespace|Name|Index`，索引从0开始
  - 区别于已支持的 defineReferenceExpression
  - 用于：CK3/VIC3/EU5
  - 移除：情况比预想的更加复杂，实际上，`array_define:Namespace|Name|Index` 自身是一个值字段表达式，`Namespace|Name|Index` 则是其中嵌套的数组定值引用表达式。
- [x] P2/FAST 2.1.10 在推断游戏类型时，同时提供描述信息，并在模组设置对话框中显示
  - 例如说明模组目录直接位于游戏创意工坊目录下，因此被推断为对应的游戏类型
  - 此时不允许在模组设置对话框中更改游戏类型
- P2/FAST 2.1.10 （已移除）~~在打开项目时，递归扫描项目中的目录，检测未配置游戏目录的模组目录，并逐个发送警告级别的通知~~
  - 跳过以 `.` 开始的目录、游戏目录和模组目录
  - 尽管已经在文件级别提供了警告级别的编辑器通知……
  - 忽略：存在其他类似的编辑器通知

### ［2026年6月13日］

- [ ] P2 新增与补充关于复杂表达式的参考文档
- [ ] P2 新增与补充关于常见术语（IDE/领域）的参考文档

### ［2026年6月15日］

- [x] P2 清理 `CHANGELOG.md`，进一步确定编写规范
- [x] P3 如果当前光标位于一个复杂表达式中，按照复杂表达式的结构来展开光标（Extend Selection） - 相关EP：`extendWordSelectionHandler`
- [ ] P4 考虑将与定值相关的语言构造/领域概念，从 *define/defineNamespace/defineVariable* 重命名为 *defineNamespaceOwner/defineNamespace/define* - 不是特别必要，但现有方案可能导致一些歧义……

### ［2026年6月16日］

- [x] P1 2.1.10 [Stellaris] 兼容 Stellaris 4.4 开始出现的 `? =`（包含空白的安全赋值运算符）
- [x] P1 2.1.10 [Stellaris] 在文法级别区分 Stellaris 4.4 中的 `? =` (SAFE_CALL_ASSIGN) 与 CK3/VIC3/EU5 中的 `?=` (SAFE_ASSIGN) & 优化格式化逻辑
- [x] P2 2.1.10 [Stellaris] 完善代码检查 `ParadoxScriptIncorrectSyntax`：验证 `? =` `?=` 是否受游戏类型支持，且左值和右值是否在文法级别合法
- [x] P3/QoL 2.1.10 提供意向和代码检查，以在作用域调用语句的安全形式（`owner ?= ...` 或 `owner? = ...`）与普通形式（`owner = ...`）之间进行转换
- [x] P3/QoL 2.1.10 提供意向和代码检查，以在作用域调用语句的链式形式（`root.owner = ...`）与嵌套形式（`root = { owner = ... }`）之间进行转换（不检查调用链是否过长）

### ［2026年6月17日］

- [x] （已移除）~~P2/QoL 提供扩展的规则选项，通过引用额外的文件位置来注入文档注入~~
  - 提案：`## documentation = some/doc.md`
  - 提案：`## inject_doc` （使用当前路径） / `## inject_doc = some/config.cwt@some/path` （使用指定路径）

### ［2026年6月19日］

- [x] P3/QoL 2.1.10 提供代码检查 `UnmatchedFileInspection`（对于脚本文件或CSV文件），以报告当前文件无法匹配到任何 `CwtFilePathMatchableConfig`（默认级别：`WARNING`） - 这意味着当前文件是特殊的，或者缺失相应的规则
- [x] P3/QoL 2.1.10 考虑与验证能否将 `ParadoxInlineScriptEditorNotificationProvider` 改为 `DeclaredInlineScriptInspection`（默认级别：`TEXT ATTRIBUTES`） - 为了对齐已有的 `UnusedInlineScriptInspection`
- [ ] P3/QoL 3.0.x 支持扩展的规则选项 `## doc = some.md`，通过引用指定的文档路径，从而包含额外的快速文档文本 - 同时补充相关参考文档
- [ ] P3/QoL 3.0.x 支持扩展的规则选项 `## inject_doc`（使用当前规则路径）或 `## inject_doc = some/config.cwt@some/path`，通过引用指定的规则文件路径上的指定规则路径，从而注入额外的快速文档文本 - 同时补充相关参考文档
- [ ] P3/QoL 3.0.x 提供对规则文件中的文件路径引用和规则路径引用的引用解析

### ［2026年6月22日］

- [ ] P3 3.3.0+ 提供数据流分析，以及相关代码检查，以检测作用域是否（在当前上下文处）存在
- [ ] P3 3.3.0+ 提供数据流分析，以及相关代码检查，以检测特定类型的动态值（event_target/variable）是否（在当前上下文处）存在
- [ ] P2 3.0.x 支持 *位置感知的数据类型*，适用于定义和本地化的匹配（如 `localisation|$_name,$_desc`），并替换掉现有的 *后缀感知的数据类型* （如 `localisation|_name,_desc`） (#340)
- [ ] P4 3.0.x 推断作用域时，可以采用 *快速推断* 策略，而非默认的 *合并所有* 策略
- [x] P3 2.2.0 支持 *并集规则（unionConfig）*，类似枚举规则（enumConfig），但是其中预定义的是任意数据表达式
- [x] P3 2.1.10 在工具菜单中新增规则系统相关的动作，并且另外提供动作，以强制刷新内置规则目录、强制刷新规则分组数据

### ［2026年6月24日］

- [x] P3 2.2.0 对于 *模式感知的数据类型*，除了 *常量/模板/ANT路径模式/正则* 之外，额外支持 *GLOB模式*
- [x] P2 2.2.0 提取 EP `incorrectSyntaxChecker` - 对齐已有的 `incorrectExpressionChecker`

### ［2026年7月4日］

- [x] P2 2.2.0 提取 EP `unresolvedExpressionDecorator`
- [ ] P2 3.0.x 重构 select API，更好的链式调用方式，更好的向上或向下查询的方式 
- [ ] P2 3.0.x 重构查询相关本地化和相关图片的API，考虑提供EP和查询器

### ［2026年7月6日］

- [ ] P2 3.0.x 考虑提供 `ParadoxModiferSearch`
- [ ] P2 3.0.x 考虑提供 `ParadoxRelatedLocalisationSearch`
- [ ] P2 3.0.x  `ParadoxRelatedImageSearch`
- [ ] P3 3.0.x 考虑进一步重构 search 和 searcher 的相关API
  - 主要思路是更好的使用方式，比如直接通过单次链式调用执行查询，无需额外创建和传入 selector 对象。
- [ ] P2 3.0.x 规范化展示（presentation）的相关 API
  - 展示：包括相关本地化和相关图片
  - 考虑提供对应的 search/searcher/provider EP。
  - 服务类应放到 `icu.windea.pls.lang.resolve`。
  -  EP 和 EP 实现应放到 `icu.windea.pls.ep.resolve.presentation`。
  - 可能并不需要提供 search/searcher，因为并非基于索引，且有多种来源和策略。
  - 最相关的本地化文本即是“展示名字/presentableName”。需要从“显示名字/localizedName”重命名。
- [ ] P2 3.0.x 规范化数据（data）的相关 API
  - 数据：接近原始状态的数据，适用于脚本/本地化/定义，可通过属性委托和数据模型类等方式获取。
  - 考虑重构和提供 dataManager/dataResolver。
  - 考虑移除已有的 dataProvider  EP。不用显式注册，如果要扩展，直接新增数据模型类即可。
  - 考虑将相关代码移到 `icu.windea.pls.lang.data`。
  - 考虑在未来另外提供 `ParadoxJsonDataManager`。
  - 目前仅按需提供特定游戏类型和定义类型的定义数据模型类。
- [ ] P2 3.0.x 规范化可视化（visualization）的相关API
  - 可视化：需要基于HTML/Swing/WebView的图形展示。
  - 考虑重构和提供 visualizationManager/visiualizationResolver/相关工具类。
  - 考虑移除已有的 visualizationProvider EP。不用显式注册。如果要扩展，直接新增组件类即可。
  - 目前仅提供（基于Swing UI的）科技卡的可视化组件类。
  - 插件自身不应也不计划直接提供更加进阶的可视化组件类。
  - 然而，仍然有可能在未来额外提供一些基于 Swing UI 的可视化组件类。
  - 考虑将相关代码移到 `icu.windea.pls.lang.visualization`。
  - 或者，作为区别于基本的本地化/图片展示的图形展示（graphPresentation）。
- [ ] P2 3.0.x 确定、验证和规范化重命名定义的逻辑
  - 解析得到文本范围完全匹配的脚本表达式元素（nameField / nameIdentifier），然后调用 elementManipulator 或者  seValue。
  - 考虑新增 `ParadoxDefinitonNameService`，或者完善 `ParadoxDefinitonService`
  - 提取 `ParadoxDefinitionManipulationService`，主要用于重命名
  - 考虑优化：可以从 typeKey 或者 nameField 出发来重命名对应的定义
- [ ] P2/MS 3.2.0 支持内联定义
  - 新增“可能声明定义”的内联脚本用法的存根索引。
  - 查询内联定义时，先查询这些内联脚本用法，在对应的脚本属性上一组缓存解析信息（inlineDefinitionInfo），依赖于自身和所有内联脚本文件。
  - 解析时，先找到最相关的那个内联脚本文件，对其进行展开，将定义名、定义类型等评估结果，以及内联脚本用法和定义模板的 PSI 指针，一同保存下来。
  - 仅缓存 inlineDefinitionInfo，后续再转为 definitionLightElement，并且直接使用 definitionInfo 作为字段，以适配已有架构。需要进行一定的重构和调整。
  - definitionLightElement 也继承自 definitionElement，但在快速文档/装订线图标等处拥有不同的行为逻辑，并且不提供内嵌提示。另外，与其他大部分 lightElement 不同，它存在一个确定的、可导航的声明处（也就是对应的内联脚本用法）。
  - 在声明了定义的内联脚本用法处，提供额外的快速文档/装订线图标，以说明这一点。
  - 如果定义名来自单一的脚本表达式元素，并且文本范围完全匹配，那么更完善的方案是，认为此内联定义是可重命名的。并且，可以考虑提供额外的代码高亮。

### ［2026年7月7日］

- [ ] P3 事件链解析应能跨越 scripted_effect
- [ ] P2 3.0.x 继续分析与优化构建索引时的性能和内存占用
- [ ] P2 3.0.x 继续分析与优化完成项目分析后的性能和内存占用

### ［2026年7月11日］

- [x] P3 3.0.0 API 变更：将 EP `unresolvedExpressionDecorator` 改为 `unresolvedExpressionChecker`
- [x] P2 3.0.0 改为为内联脚本文件提供编辑器通知，而非代码检查，以避免无法适用于非项目文件（回退到 `ParadoxInlineScriptEditorNotificationProvider`，移除 `DeclaredInlineScriptInspection`）
- [ ] P2 3.0.x 完善脚本文件的 lexer：更好的对可选空白和高级插值语法（如 `a_$p$_[[p]b]`）的支持
- [x] P2 3.0.0 完善本地化文件的 lexer：更好的对可选空白和高级插值语法（如 `a_$p$_[b]`）的支持
- [ ] P3 对于作为上下文的规则以及匹配的规则，有时需要展开并集（`union[x]`）和别名（`alias_keys_field[x]`）

## ［2026年7月14日］

- [ ] P4 快速文档中有渲染较大的dds图片时，快速文档的 popup 可能会发生抖动
- [ ] P2 3.0.1 对于 `alias[x] = union[y]`，匹配逻辑不符合预期

## ［2026年7月16日］

- [x] P3 3.0.1 移除 FastList 等别名，需要明确 fastutil 的对象集合并不一定意味着性能优势，有时可能更适合使用 ImmutableList 或 SmartList，并且IDE目前并不能很好地支持别名导入，同步记录架构决策
- [x] P3 3.0.1 （忽略）~~评估是否可以在原始的索引数据（indexData 和 stub）中直接使用数组，以最大化性能和最小化内存占用，同步记录架构决策~~ - 然而构建过程通常是动态的，只是在反序列化时大小是固定的 - 仍然保持使用 ImmutableList，同步记录架构决策
- [x] P3 3.0.1 接上面两条，记录关于“集合选型”的整体上的架构决策
  - 这是偏细节的决策，因此目前不一并写入代码规范文档。
- [x] P2 3.0.1 定义名应允许为任意字符串字面量（而非仅限合法的标识符） (#369)
  - 这通常见于定义名来自属性值的情况。
  - 特别的，需要兼容包含空白的情况，进行代码补全时按需加上引号。
- [x] P2 3.0.1 考虑将 `ROADMAP.md` 移到 `documents/notes/开发路线.md`，将原本的 `开发路线.md` 重命名为 `想法记录.md`
  - 这仍然是一个更偏维护笔记性质的文档，并且是几乎全部用中文写的，出于对齐的目的，最好同样归为维护者笔记。
  - “开发路线”笔记用于记录已经放到开发路线图上的，相比更新日志拥有更多细节的更新事项，并且主要按照版本归类。这里涉及的细节如果过多，可以考虑移到且链接到“架构决策”笔记或“问题解决”笔记，乃至单独的参考文档和需求文档。这份笔记是进一步整理后的。
  - “想法记录”笔记用于记录随时产生的，主要以待办事项的形式出现的想法，并且主要按日期归类。这份笔记并不会包括所有形式的想法，也不会包括所有已记录的待办事项，只是选择公开的、相对明显和确定的那些。