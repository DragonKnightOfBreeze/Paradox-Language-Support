# 笔记：架构决策

## UI

## 通用

### 资源相关

#### 图标设计

- 更新版本：v2.1.8
- 更新日期：2026.05.05

参见：[图标设计.md](/documents/references/图标设计.md)

#### 配色方案

- 更新版本：v2.1.8
- 更新日期：2026.05.05

参见：[配色方案.md](/documents/references/配色方案.md)

## 后端

### 通用

#### 重构 `CwtDataTypes` `CwtConfigTypes` - 改为偏好使用 lambda 式构建器

- 版本：v2.1.8
- 日期：2026.05.04

说明：
- 改为偏好使用 lambda 式构建器，而非多行的链式构建器。
- `.suffixAware()` `.reference()` 这样的“重要信息”需要紧随 `builder()` 之后，其余细节信息放到 `builder {...}` 中。
- 如果没有细节信息，直接使用单行的链式构建器即可。

目的：
- 为了适用代码折叠，从而隐藏细节，方便查看。

相关代码位置：
- `icu.windea.pls.config.CwtConfigTypes`
- `icu.windea.pls.config.CwtDataTypes`

#### 重构作用域和作用域方法 - 改为偏好使用成员方法作为作用域方法，而非带上下文参数的扩展方法

- 版本：v2.1.8
- 日期：2026.05.08

说明：
- 这里的作用域指的是某种接口或类，对应某种限定的API范围，某些情况下还带有自己的上下文。`SequenceScope` 和 `CoroutineScope` 都是典型的作用域。
- 区别于普通的工具方法和扩展方法，作用域方法只能在对应的作用域中使用。例如，将作用域对象作为 `this`，或者上下文参数 `context(scope: Scope)`。
- 改为偏好使用成员方法作为作用域方法，而非带上下文参数的扩展方法。除非不存在一个明确的可控制的作用域（类/接口）。

目的：
- 即使上下文中不存在符合条件的作用域对象，IDE 仍会补全作为扩展方法的那些作用域方法。
- 改为使用成员方法的话，可以避免这种非理想的行为，并且允许隐藏实现细节。

相关代码位置：
- `icu.windea.pls.config.select.CwtConfigSelectScope`
- `icu.windea.pls.lang.select.ParadoxPsiSelectScope`

#### 重构 path/expression/complexExpression/config/configExpression - 更改解析器与解析方法的代码风格

- 版本：V2.1.10
- 日期：2026.06.14

说明：
- 将对外的解析方法改为模型接口上的静态方法，委托给解析器类（可能是单例对象，可能是私有的）的对应方法。
- 解析器类通常是同文件中的私有类，少数情况下也可以是不同文件中的公开类。
- 偏好上，将同文件中的私有实现方法放到公开接口方法之后，并且使用 `region...endregion` 注释包围。
- 偏好上，将文档注释放到接口代码上，实现代码（以及公开的解析器类的代码）通常不需要文档注释。

目的：
- 为了统一代码风格。

示例（不需要公开解析类的情况）：

```kotlin
interface Expression {
    val value: String
    
    companion object {
        @JvmStatic
        fun resolve(text: String): Expression = ExpressionResolver.resolve(text)
    }
}

// region Implementations

private object ExpressionResolver {
    fun resolve(text: String): Expression {
        return ExpressionImpl(text)
    }
}

private data class ExpressionImpl(override val value: String)

// endregion
```

相关代码位置：
- `icu.windea.pls.model.scope`
- `icu.windea.pls.model.paths`
- `icu.windea.pls.model.expressions`
- `icu.windea.pls.config.config`
- `icu.windea.pls.config.configExpression`
- `icu.windea.pls.expression.expressions` (since v3.1.0)

### IDE

#### 刷新文件与重解析文件

- 版本：v2.1.9
- 日期：2026.05.12

刷新文件：
- `DaemonCodeAnalyzer.getInstance(project).restart()` - 刷新指定项目的所有文件的高亮和内嵌提示
- `DaemonCodeAnalyzer.getInstance(project).restart(file)` - 刷新指定项目的指定文件的高亮（不会刷新内嵌提示）
- `InlayHintsPassFactoryInternal.clearModificationStamp(editor)` - 刷新指定文件的内嵌提示
- 可以在 readAction 中使用，可以在新开启的协程中使用
- 刷新高亮和内嵌提示

重新解析文件：
- `FileContentUtilCore.reparseFiles` - 重新解析一组文件
- writeAction，通常需要在 edt 中执行
- 之后会自动重建索引、刷新高亮和内嵌提示
- 同时需要适当地清空缓存、更新更改追踪器

相关代码位置：
- `icu.windea.pls.ide.analysis.PlsAnalysisManager`
- `com.intellij.codeInsight.daemon.DaemonCodeAnalyzer`
- `com.intellij.util.FileContentUtilCore`
- `com.intellij.codeInsight.daemon.impl.InlayHintsPassFactoryInternal`

## 语言功能支持

#### 语句操作服务 & 相关的意向动作和代码检查

- 版本：v2.1.10
- 日期：2026.06.16
- TODO

说明：
- 提供数种语句操作服务（statementManipulationService），以及相关的意向动作和代码检查，以便快速乃至批量地切换语句形式
- conditional - propertyForm (`PARAM = $PARAM|no$`) VS blockForm (`[[PARAM] PARAM = $PARAM$ ]`)
- scopeCall - normalForm (`exists = owner owner = {...}`) VS safeForm (use `?=` or `? =`)
- scopeCall - chainedForm (`root.owner = { ... }`) VS nestedForm (`root = { owner = ... }`)

关键代码位置：
- conditional
  - [x] `icu.windea.pls.lang.manipulation.ParadoxConditionalStatementManipulationService`
  - [x] `icu.windea.pls.lang.intentions.script.ConditionalStatementToPropertyFormIntention`
  - [x] `icu.windea.pls.lang.intentions.script.ConditionalStatementToBlockFormIntention`
- scopeCall
  - [x] `icu.windea.pls.lang.manipulation.ParadoxScopeCallStatementManipulationService`
  - [x] `icu.windea.pls.lang.intentions.script.ScopeCallStatementToNormalFormIntention`
  - [x] `icu.windea.pls.lang.intentions.script.ScopeCallStatementToSafeFormIntention`
  - [x] `icu.windea.pls.lang.inspections.script.statement.ScopeCallStatementToNormalFormInspection` - 默认级别：INFORMATION
  - [x] `icu.windea.pls.lang.inspections.script.statement.ScopeCallStatementToSafeFormInspection` - 默认级别：WEAK_WARNING
  - [ ] `icu.windea.pls.lang.intentions.script.ScopeCallStatementToChainedFormIntention`
  - [ ] `icu.windea.pls.lang.intentions.script.ScopeCallStatementToNestedFormIntention`

### 语言构造支持 / language construct support

#### 增强对定值（define）的支持

- 版本：v2.1.8
- 日期：2026.05.04

说明：
- 定值（define）分为定值的命名空间和变量，它们固定声明于游戏目录的 `common/defines` 子目录中的带 `.txt` 扩展名的脚本文件中。
- 引入对应的定值规则（defineConfig），从而为定值的命名空间和变量提供快速文档文本和规则上下文。
- 定值是在（游戏目录中的）脚本文件中声明的。普通导航会跳转到覆盖项，相关规则导航才会跳转到对应的定值规则。
- 理论上，定值是取决于具体的游戏的，并且会在游戏目录中声明并列出所有可用项。
- 但是，插件目前不通过定值规则检查定值的命名空间和变量的名字的合法性（仍然检查其声明结构的合法性，如同定义和定义注入一样）。
- 插件会强制忽略名为 `define` 或 `defines` 的类型规则和声明规则（参见：`icu.windea.pls.ep.config.config.CwtForcedConfigFilterProvider`）。

关键代码位置：
- `icu.windea.pls.lang.util.ParadoxDefineManager`
- `icu.windea.pls.model.ParadoxDefineInfo`
- `icu.windea.pls.model.ParadoxDefineNamespaecInfo`
- `icu.windea.pls.model.ParadoxDefineVariableInfo`

相关代码位置（规则）：
- `icu.windea.pls.config.config.delegated.CwtDefineConfig`
- `icu.windea.pls.config.config.delegated.CwtDefineNamespaceConfig`
- `icu.windea.pls.config.config.delegated.CwtDefineVariableConfig`

相关代码位置（IDE 功能）：
- `icu.windea.pls.lang.index.ParadoxDefineNamespaceIndex` - 存根索引
- `icu.windea.pls.lang.index.ParadoxDefineVariableIndex` - 存根索引
- `icu.windea.pls.lang.search.ParadoxDefineNamespaceSearch` - 查询
- `icu.windea.pls.lang.search.ParadoxDefineVariableSearch` - 查询
- `icu.windea.pls.lang.codeInsight.navigation.GotoDefineVariablesAction` - 导航动作
- `icu.windea.pls.lang.codeInsight.navigation.GotoDefineVariablesActionHandler` - 导航动作
- `icu.windea.pls.lang.codeInsight.markers.script.ParadoxDefineVariableLineMarkerProvider` - 装订线图标
- `icu.windea.pls.lang.search.searchers.implementations.ParadoxDefineVariableImplementationsSearcher` - 实现查询
- `icu.windea.pls.lang.codeInsight.completion.script.ParadoxDefineNameCompletionProvider` - 代码补全
- `icu.windea.pls.lang.inspections.overrides.OverrideForDefineVariableInspection` - 代码检查（重载）
- `icu.windea.pls.lang.inspections.overrides.IncorrectOverrideForDefineVariableInspection` - 代码检查（不正确的重载）
- `icu.windea.pls.ep.resolve.config.CwtDefineVariableConfigContextProvider` - 提供规则上下文

#### 增强对数据类型 `ShaderEffect` 和 `MeshLocator` 的支持

- 版本：v2.1.9
- 日期：2026.05.14

说明：
- `ShaderEffect` - 匹配对着色器效果（shader effect）的引用。目前将这些引用视为动态引用，尽管其声明实际上位于 `.shader` 文件中。
- `MeshLocator` - 匹配对网格定位器（mesh locator）的引用。目前将这些引用视为动态引用，尽管其声明实际上位于 `.mesh` 文件中。
- “动态引用”意味着不存在实际上的声明处，仅区分读写访问，如同动态值一样。
- 对于以上两种数据类型来说，仅存在读访问（即引用）。

相关代码位置：
- `icu.windea.pls.config.CwtDataTypes.ShaderEffect`
- `icu.windea.pls.config.CwtDataTypes.MeshLocator`
- `icu.windea.pls.lang.psi.light.ParadoxShaderEffectLightElement`
- `icu.windea.pls.lang.psi.light.ParadoxMeshLocatorLightElement`
- `icu.windea.pls.ep.resolve.expression.ParadoxScriptShaderEffectExpressionSupport`
- `icu.windea.pls.ep.resolve.expression.ParadoxScriptMeshLocatorExpressionSupport`

#### 完善对 CSV 文件与 rowConfig 的支持

- 版本：v2.2.0
- TODO

相关问题：
- https://github.com/DragonKnightOfBreeze/Paradox-Language-Support/issues/314

说明：
- 表头列的列名（columnKey/columnName）可以重复，因此在解析规则数据和语义解析时，应基于列索引，而非列名。
- 表头列的最后一列，以及文件中的最后一行，可能需要在语义解析时忽略，因此需要提供对应的规则字段。
- 需要调整行规则的格式，调整其字段。
- 需要调整相关代码检查，新增/移除某些代码检查。

相关代码位置：
- `icu.windea.pls.config.config.delegated.CwtRowConfig`
- `icu.windea.pls.lang.util.ParadoxCsvManager`
- `icu.windea.pls.lang.inspections.csv`

调整后的代码检查：
- `IncorrectColumnNameInspection` - 表头行的列名不正确（要求存在对应的行规则，按列索引和列名进行匹配）。
- `IncorrectColumnSizeInspection` - 某行的列的数量与表头行的列的数量不一致（要求存在对应的行规则）。
- `UnresolvedExpressionInspection` - 某个列存在对应的列规则，但是无法匹配（因此视为无法解析的表达式）。
- `IncorrectExpressionInspection` - 某个列存在对应的列规则，，但是检查发现不正确（因此视为不正确的表达式）。

### 分析层 / analysis

#### 优化 rootInfo 和 rootMetadata 的解析逻辑

- 版本：v2.1.8
- 日期：2026.05.03

说明：
- 解析这些数据时，不应也不需要依赖 VFS，因此也无需 readAction
- 直接基于 `Path` 定位文件，解析时不要依赖文件的 PSI
- 相关的 EP 也改为直接传入 `Path` 而非 `VirtualFile`
- `ParadoxRootMetadata` 不再使用 `rootFile: VirtualFile` 作为属性，而是改为使用 `rootPath: Path`

关键代码位置：
- `icu.windea.pls.model.ParadoxRootInfo`
- `icu.windea.pls.model.analysis.ParadoxRootMetadata`
- `icu.windea.pls.model.analysis.ParadoxRootMetadataInfo`
- `icu.windea.pls.ep.analysis.ParadoxRootMetadataProvider` - EP
- `icu.windea.pls.lang.analysis.ParadoxRootMetadataUtil` - 主要用于获取路径和解析信息

### 工具 / tools

#### 特殊路径与链接

- 版本：v2.1.9
- 日期：2026.05.16

说明：
- 提供API、动作和UI，以便获取、复制与打开各种特殊的路径与链接（如游戏目录、Steam游戏页面链接）

关键代码位置：
- `icu.windea.pls.lang.tools.SpecialPathService` - 服务接口
- `icu.windea.pls.lang.tools.SpecialPathServiceImpl` - 服务实现类
- `icu.windea.pls.lang.tools.SpecialUrlService` - 服务接口
- `icu.windea.pls.lang.tools.SpecialUrlServiceImpl` - 服务实现类
- `icu.windea.pls.ep.tools.SpecialPathProvider` - EP
- `icu.windea.pls.ep.tools.SpecialUrlProvider` - EP

## 测试

### 通用

### 快照测试

#### 基于彩蛋目录的快照测试

- 更新版本：v2.1.9
- 更新日期：2026.05.20

说明：
- `icu.windea.pls.test.chronicle.ChronicleSnapshotTest` - 基类。
- `icu.windea.pls.test.chronicle.ChronicleInspectionsSnapshotTest` - 基于各种默认启用的代码检查的快照测试。
- `icu.windea.pls.test.chronicle.ChronicleAnnotatedSnapshotTest` - 基于数种注解渲染器的快照测试。

这是一个长期工作，随着时间的推进和版本的演进，需要持续完善。丰富测试数据文件的内容，覆盖更多要测试的情况。